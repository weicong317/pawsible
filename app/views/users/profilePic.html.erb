<%= form_with scope: :profile_pic, url: profile_pic_upload_path(@user.id), method: :put, local: true do |form| %>
  <%= form.label :profile_pic, "Profile Picture"%>
  <%= form.file_field :profile_pic %>
  <%= form.submit "Change Profile Picture" %>
<% end %>